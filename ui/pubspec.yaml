name: jarvis_ui
description: JARVIS Android UI with standalone Android voice activation and AI connection
publish_to: "none"
version: 0.1.2

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_selector: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
